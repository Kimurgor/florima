{
  "name": "florima",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hugo --minify --cleanDestinationDir",
    "deploy": "hugo --minify --cleanDestinationDir && npx wrangler pages deploy public --project-name=florima",
    "start": "hugo server --bind 127.0.0.1 --port 8088",
    "serve": "hugo server --bind 127.0.0.1 --port 8088",
    "test:function": "node tests/contact-function.test.js"
  }
}
